'use client'

import { useState } from 'react'
import { useChat } from '@ai-sdk/react'
import { DefaultChatTransport } from 'ai'
import { ArrowRight, Brain, Check, ChevronDown, ClipboardCheck, FileText, Mail, Menu, MessageCircle, Send, Settings, Sparkles, Users, X, Zap } from 'lucide-react'

const features = [
  { icon: Mail, number: '01', title: 'Smart Email Generator', description: 'Draft polished, context-aware emails in seconds. Choose the tone, share your intent, and let Aura Flow handle the rest.', bullets: ['Professional email drafts', 'Tone and style controls', 'Time-saving automation'], accent: 'from-blue-500 to-cyan-400' },
  { icon: FileText, number: '02', title: 'Meeting Notes Summarizer', description: 'Turn long meeting transcripts into concise summaries with decisions, highlights, and clear next steps.', bullets: ['Key points extracted', 'Decisions and action items', 'Readable summaries'], accent: 'from-purple-500 to-fuchsia-400' },
  { icon: ClipboardCheck, number: '03', title: 'AI Task Planner', description: 'Transform goals into structured action plans. Prioritise tasks, set timelines, and make progress visible.', bullets: ['Smart task breakdowns', 'Priority recommendations', 'Practical timelines'], accent: 'from-indigo-500 to-blue-400' },
]
const benefits = ['Reduced time spent on repetitive work', 'Clearer communication across teams', 'More consistent and actionable outputs', 'A focused workflow built around your goals']
const teammates = [{ name: 'Maya Chen', role: 'Product design', initials: 'MC', color: 'bg-purple-500' }, { name: 'Jordan Lee', role: 'Operations', initials: 'JL', color: 'bg-blue-500' }, { name: 'Sam Okafor', role: 'Engineering', initials: 'SO', color: 'bg-emerald-500' }]

type TeamMessage = { name: string; initials: string; text: string; time: string; color: string }

function Assistant() {
  const { messages, sendMessage, status, error } = useChat({ transport: new DefaultChatTransport({ api: '/api/chat' }) })
  const [input, setInput] = useState('')
  const busy = status === 'submitted' || status === 'streaming'
  const submit = (text = input) => { if (!text.trim() || busy) return; sendMessage({ text }); setInput('') }
  return <section id="assistant" className="bg-slate-950 py-20 text-white sm:py-28"><div className="mx-auto grid max-w-7xl gap-10 px-5 sm:px-8 lg:grid-cols-[.8fr_1.2fr] lg:px-12"><div><p className="mb-4 text-sm font-semibold uppercase tracking-widest text-blue-300">04 / Try the assistant</p><h2 className="text-4xl font-semibold tracking-tight sm:text-5xl">Meet <span className="text-blue-300">Aura Flow.</span></h2><p className="mt-6 max-w-lg leading-7 text-slate-300">A live AI help desk for the moments that slow work down. Ask it to write, summarise, plan, or turn your thoughts into a clear next step.</p><div className="mt-8 flex flex-wrap gap-2">{['Draft an email', 'Summarise meeting notes', 'Plan my week', 'Prepare for Notion'].map((prompt) => <button key={prompt} onClick={() => submit(prompt)} className="rounded-full border border-white/15 bg-white/5 px-4 py-2 text-xs text-slate-200 transition hover:bg-white/10">{prompt}</button>)}</div><div className="mt-8 flex items-center gap-3 text-sm text-slate-400"><span className="h-2 w-2 rounded-full bg-emerald-400" /> Live through Vercel AI Gateway</div></div><div className="overflow-hidden rounded-3xl border border-white/10 bg-white/[.06] shadow-2xl"><div className="flex items-center justify-between border-b border-white/10 px-5 py-4"><div className="flex items-center gap-3"><span className="gradient-primary flex h-9 w-9 items-center justify-center rounded-xl"><Brain size={18} /></span><div><p className="text-sm font-semibold">Aura Flow</p><p className="text-xs text-slate-400">Workplace productivity copilot</p></div></div><span className="text-xs text-emerald-300">Online</span></div><div className="flex min-h-[340px] flex-col gap-4 p-5">{messages.length === 0 && <div className="rounded-2xl bg-blue-500/10 p-4 text-sm leading-6 text-blue-100">Hi, I&apos;m Aura Flow. Tell me what you need to get done and I&apos;ll help you make it clearer, faster, and more actionable.</div>}{messages.map((message) => <div key={message.id} className={`max-w-[88%] rounded-2xl p-4 text-sm leading-6 ${message.role === 'user' ? 'self-end bg-blue-600 text-white' : 'bg-white/10 text-slate-100'}`}><p className="mb-1 text-[10px] font-semibold uppercase tracking-wider text-slate-400">{message.role === 'user' ? 'You' : 'Aura Flow'}</p>{message.parts?.map((part, index) => part.type === 'text' ? <span key={index}>{part.text}</span> : null)}</div>)}{busy && <div className="self-start rounded-2xl bg-white/10 px-4 py-3 text-sm text-slate-300">Aura Flow is thinking...</div>}{error && <p className="text-xs text-rose-300">Aura Flow could not respond right now. Please try again.</p>}</div><form onSubmit={(event) => { event.preventDefault(); submit() }} className="flex gap-2 border-t border-white/10 p-4"><input value={input} onChange={(event) => setInput(event.target.value)} placeholder="Ask Aura Flow for help..." className="min-w-0 flex-1 rounded-xl border border-white/10 bg-white/10 px-4 py-3 text-sm text-white outline-none placeholder:text-slate-500 focus:border-blue-400" aria-label="Ask Aura Flow" /><button disabled={busy} aria-label="Send message" className="rounded-xl bg-blue-500 px-4 text-white transition hover:bg-blue-400 disabled:opacity-50"><Send size={17} /></button></form></div></div></section>
}

function TeamRoom() {
  const [selected, setSelected] = useState(teammates[0])
  const [text, setText] = useState('')
  const [messages, setMessages] = useState<TeamMessage[]>([{ name: 'Maya Chen', initials: 'MC', text: 'I added the new launch notes. Aura Flow can turn them into a task list when you are ready.', time: '09:41', color: 'bg-purple-500' }])
  const send = () => { if (!text.trim()) return; setMessages((current) => [...current, { name: 'You', initials: 'YO', text, time: 'Now', color: 'bg-blue-600' }]); setText('') }
  return <section id="team" className="border-y border-border bg-slate-50 py-20 sm:py-28"><div className="mx-auto max-w-7xl px-5 sm:px-8 lg:px-12"><div className="mb-10 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mb-4 text-sm font-semibold uppercase tracking-widest text-blue-600">05 / Team communication</p><h2 className="text-4xl font-semibold tracking-tight sm:text-5xl">Work together, <span className="text-purple-600">in context.</span></h2></div><p className="max-w-md leading-7 text-muted-foreground">Share updates with your team, then bring Aura Flow into the conversation to turn discussion into action.</p></div><div className="grid overflow-hidden rounded-3xl border border-border bg-white shadow-sm lg:grid-cols-[260px_1fr]"><aside className="border-b border-border p-4 lg:border-b-0 lg:border-r"><div className="mb-4 flex items-center justify-between"><p className="text-sm font-semibold">People</p><span className="rounded-full bg-emerald-50 px-2 py-1 text-[10px] font-semibold text-emerald-700">3 online</span></div><div className="space-y-2">{teammates.map((person) => <button key={person.name} onClick={() => setSelected(person)} className={`flex w-full items-center gap-3 rounded-2xl p-3 text-left transition ${selected.name === person.name ? 'bg-blue-50' : 'hover:bg-slate-50'}`}><span className={`flex h-9 w-9 items-center justify-center rounded-full text-xs font-semibold text-white ${person.color}`}>{person.initials}</span><span><span className="block text-sm font-medium">{person.name}</span><span className="block text-xs text-muted-foreground">{person.role}</span></span><span className="ml-auto h-2 w-2 rounded-full bg-emerald-400" /></button>)}</div></aside><div className="flex min-h-[390px] flex-col"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div className="flex items-center gap-3"><MessageCircle size={18} className="text-blue-600" /><div><p className="text-sm font-semibold">Project workspace</p><p className="text-xs text-muted-foreground">Chat with {selected.name}</p></div></div><button className="rounded-lg border border-border px-3 py-2 text-xs font-medium text-muted-foreground hover:bg-muted">Invite teammate</button></div><div className="flex-1 space-y-4 p-5">{messages.map((message, index) => <div key={`${message.time}-${index}`} className="flex gap-3"><span className={`flex h-8 w-8 shrink-0 items-center justify-center rounded-full text-[10px] font-semibold text-white ${message.color}`}>{message.initials}</span><div><div className="flex items-baseline gap-2"><span className="text-sm font-semibold">{message.name}</span><span className="text-[10px] text-muted-foreground">{message.time}</span></div><p className="mt-1 max-w-xl text-sm leading-6 text-muted-foreground">{message.text}</p></div></div>)}</div><div className="border-t border-border p-4"><div className="flex gap-2"><input value={text} onChange={(event) => setText(event.target.value)} onKeyDown={(event) => { if (event.key === 'Enter' && !event.nativeEvent.isComposing && event.keyCode !== 229) send() }} placeholder={`Message ${selected.name}...`} className="min-w-0 flex-1 rounded-xl border border-border bg-slate-50 px-4 py-3 text-sm outline-none focus:border-blue-400" aria-label="Team message" /><button onClick={send} className="rounded-xl bg-slate-950 px-4 text-white hover:bg-blue-700" aria-label="Send team message"><Send size={17} /></button></div><p className="mt-2 text-[11px] text-muted-foreground">Team room preview: messages are available during this session.</p></div></div></div></div></section>
}

export default function Page() {
  const [menuOpen, setMenuOpen] = useState(false)
  const [settingsOpen, setSettingsOpen] = useState(false)
  return <main className="min-h-screen overflow-hidden bg-background text-foreground"><nav className="sticky top-0 z-50 border-b border-border/70 bg-white/85 backdrop-blur-xl dark:bg-slate-950/85"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 sm:px-8 lg:px-12"><a href="#home" className="flex items-center gap-3 font-semibold tracking-tight"><span className="gradient-primary flex h-9 w-9 items-center justify-center rounded-xl text-white shadow-lg shadow-blue-500/20"><Brain size={20} /></span><span className="text-sm">Aura <span className="text-blue-600">Flow</span></span></a><div className="hidden items-center gap-8 text-sm font-medium text-muted-foreground md:flex"><a className="transition-colors hover:text-primary" href="#about">About</a><a className="transition-colors hover:text-primary" href="#features">Features</a><a className="transition-colors hover:text-primary" href="#assistant">Try Aura Flow</a><a className="transition-colors hover:text-primary" href="#team">Team room</a></div><button onClick={() => setSettingsOpen(true)} className="hidden items-center gap-2 rounded-full border border-border px-4 py-2 text-sm font-medium transition hover:bg-muted md:flex"><Settings size={15} /> Settings</button><button aria-label={menuOpen ? 'Close menu' : 'Open menu'} className="rounded-lg p-2 md:hidden" onClick={() => setMenuOpen(!menuOpen)}>{menuOpen ? <X size={21} /> : <Menu size={21} />}</button></div>{menuOpen && <div className="border-t border-border bg-white px-5 py-4 dark:bg-slate-950 md:hidden"><div className="flex flex-col gap-4 text-sm font-medium"><a href="#about" onClick={() => setMenuOpen(false)}>About</a><a href="#features" onClick={() => setMenuOpen(false)}>Features</a><a href="#assistant" onClick={() => setMenuOpen(false)}>Try Aura Flow</a><a href="#team" onClick={() => setMenuOpen(false)}>Team room</a><button className="flex items-center gap-2 text-left" onClick={() => { setMenuOpen(false); setSettingsOpen(true) }}><Settings size={15} /> Settings</button></div></div>}</nav>
<section id="home" className="relative mx-auto max-w-7xl px-5 pb-20 pt-16 sm:px-8 sm:pt-24 lg:px-12 lg:pb-28 lg:pt-32"><div className="pointer-events-none absolute -right-36 -top-40 h-[480px] w-[480px] rounded-full bg-blue-300/20 blur-3xl" /><div className="relative grid items-center gap-14 lg:grid-cols-[1.1fr_.9fr] lg:gap-20"><div className="slide-in-left"><div className="mb-7 inline-flex items-center gap-2 rounded-full border border-blue-200 bg-blue-50 px-3 py-1.5 text-xs font-semibold text-blue-700"><Sparkles size={14} /> AI Skills Accelerator Programme</div><h1 className="max-w-3xl text-5xl font-semibold leading-[1.05] tracking-[-0.04em] sm:text-6xl lg:text-7xl">Work smarter with <span className="bg-gradient-to-r from-blue-600 via-indigo-600 to-purple-600 bg-clip-text text-transparent">Aura Flow.</span></h1><p className="mt-7 max-w-2xl text-lg leading-8 text-muted-foreground sm:text-xl">An intelligent workplace assistant that automates email drafting, meeting summarization, and task planning using Artificial Intelligence.</p><div className="mt-9 flex flex-col gap-3 sm:flex-row"><a href="#assistant" className="gradient-primary inline-flex items-center justify-center gap-2 rounded-xl px-6 py-3.5 text-sm font-semibold text-white shadow-xl shadow-blue-500/20 transition-transform hover:scale-[1.03]">Try Aura Flow <ArrowRight size={17} /></a><a href="#team" className="inline-flex items-center justify-center gap-2 rounded-xl border border-border bg-white px-6 py-3.5 text-sm font-semibold transition-colors hover:bg-muted"><Users size={17} /> Meet the team room</a></div></div><div className="slide-in-right relative mx-auto w-full max-w-md"><div className="gradient-secondary absolute inset-5 rounded-[2.5rem] opacity-20 blur-2xl" /><div className="relative overflow-hidden rounded-[2rem] border border-white/70 bg-slate-950 p-5 shadow-2xl"><div className="mb-10 flex items-center justify-between text-xs text-slate-400"><span>AURA FLOW / WORKSPACE</span><span className="flex items-center gap-1.5"><span className="h-1.5 w-1.5 rounded-full bg-emerald-400" /> Online</span></div><div className="mb-3 text-sm text-slate-400">Today&apos;s focus</div><div className="text-2xl font-semibold text-white">Make progress visible.</div><div className="mt-8 space-y-3"><div className="rounded-2xl bg-white/10 p-4"><div className="flex items-center gap-3"><span className="rounded-lg bg-blue-500/20 p-2 text-blue-300"><Zap size={16} /></span><div><div className="text-sm font-medium text-white">Plan next steps</div><div className="mt-1 text-xs text-slate-400">3 priorities ready</div></div><ChevronDown className="ml-auto rotate-[-90deg] text-slate-500" size={15} /></div></div><div className="rounded-2xl bg-white/10 p-4"><div className="flex items-center gap-3"><span className="rounded-lg bg-purple-500/20 p-2 text-purple-300"><MessageCircle size={16} /></span><div><div className="text-sm font-medium text-white">Team room</div><div className="mt-1 text-xs text-slate-400">2 new updates</div></div><ChevronDown className="ml-auto rotate-[-90deg] text-slate-500" size={15} /></div></div></div></div></div></div></section>
<section id="about" className="border-y border-border bg-white py-20 sm:py-28"><div className="mx-auto max-w-7xl px-5 sm:px-8 lg:px-12"><div className="grid gap-12 lg:grid-cols-[.75fr_1.25fr] lg:gap-24"><div><p className="mb-4 text-sm font-semibold uppercase tracking-widest text-blue-600">01 / About the project</p><h2 className="text-4xl font-semibold tracking-tight sm:text-5xl">Making space for <span className="text-purple-600">higher-value work.</span></h2></div><div className="grid gap-8 text-muted-foreground"><div><h3 className="mb-2 text-lg font-semibold text-foreground">The problem</h3><p className="leading-7">Professionals spend significant time on repetitive tasks such as drafting emails, summarizing meetings, and planning schedules. Aura Flow helps automate these processes and improve efficiency.</p></div><div><h3 className="mb-2 text-lg font-semibold text-foreground">The objective</h3><p className="leading-7">Create a practical, approachable AI companion that removes friction from everyday work without removing human judgement from the process.</p></div><div className="grid gap-3 border-l-2 border-purple-400 pl-5"><p className="font-medium leading-7 text-foreground">Why this solution matters</p><p className="leading-7">When routine work becomes lighter, people get more time for creativity, collaboration, and the decisions that move organisations forward.</p></div></div></div></div></section>
<section id="features" className="mx-auto max-w-7xl px-5 py-20 sm:px-8 sm:py-28 lg:px-12"><div className="mb-12 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mb-4 text-sm font-semibold uppercase tracking-widest text-blue-600">02 / Core capabilities</p><h2 className="text-4xl font-semibold tracking-tight sm:text-5xl">One assistant.<br /><span className="text-muted-foreground">Three ways to work better.</span></h2></div><p className="max-w-sm leading-7 text-muted-foreground">Thoughtfully designed AI tools that fit into the flow of your work, not the other way around.</p></div><div className="grid gap-5 lg:grid-cols-3">{features.map((feature) => { const Icon = feature.icon; return <article key={feature.number} className="card-hover group relative overflow-hidden rounded-3xl border border-border bg-white p-7 shadow-sm"><div className={`mb-16 flex h-12 w-12 items-center justify-center rounded-2xl bg-gradient-to-br ${feature.accent} text-white shadow-lg`}><Icon size={22} /></div><span className="absolute right-7 top-7 text-xs font-semibold text-muted-foreground">{feature.number}</span><h3 className="text-xl font-semibold">{feature.title}</h3><p className="mt-3 min-h-[84px] text-sm leading-6 text-muted-foreground">{feature.description}</p><ul className="mt-6 space-y-3 border-t border-border pt-5 text-sm">{feature.bullets.map((bullet) => <li key={bullet} className="flex items-center gap-2"><Check size={15} className="text-blue-600" />{bullet}</li>)}</ul></article> })}</div></section>
<Assistant /><TeamRoom /><section id="impact" className="gradient-primary relative overflow-hidden py-20 text-white sm:py-24"><div className="relative mx-auto max-w-7xl px-5 sm:px-8 lg:px-12"><div className="grid items-center gap-12 lg:grid-cols-2"><div><p className="mb-4 text-sm font-semibold uppercase tracking-widest text-blue-100">06 / Expected impact</p><h2 className="text-4xl font-semibold tracking-tight sm:text-5xl">Productivity with a <span className="text-blue-100">human edge.</span></h2><p className="mt-6 max-w-xl leading-7 text-blue-50">The goal isn&apos;t to automate people out of the picture. It&apos;s to give them a clearer starting point, fewer repetitive tasks, and more time to do their best work.</p></div><ul className="space-y-4">{benefits.map((benefit) => <li key={benefit} className="flex items-center gap-4 rounded-2xl border border-white/15 bg-white/10 p-4"><span className="rounded-full bg-white/15 p-2"><Check size={17} /></span><span className="text-sm font-medium">{benefit}</span></li>)}</ul></div></div></section>
<footer id="contact" className="border-t border-border bg-white py-12"><div className="mx-auto flex max-w-7xl flex-col gap-6 px-5 sm:px-8 md:flex-row md:items-center md:justify-between lg:px-12"><div><div className="flex items-center gap-3 font-semibold"><span className="gradient-primary flex h-8 w-8 items-center justify-center rounded-lg text-white"><Brain size={16} /></span>Aura Flow</div><p className="mt-3 text-sm text-muted-foreground">AI Skills Accelerator Programme project</p></div><div className="flex items-center gap-4 text-sm text-muted-foreground"><span className="flex items-center gap-2"><Users size={15} /> Collaborative AI workspace</span><span>© 2026</span></div></div></footer>
{settingsOpen && <div className="fixed inset-0 z-[60] flex items-center justify-center bg-slate-950/50 p-5" role="dialog" aria-modal="true" aria-labelledby="settings-title"><div className="w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-widest text-blue-600">Aura Flow</p><h2 id="settings-title" className="mt-1 text-2xl font-semibold">Your settings</h2></div><button onClick={() => setSettingsOpen(false)} aria-label="Close settings" className="rounded-full p-2 hover:bg-muted"><X size={18} /></button></div><div className="mt-6 space-y-5"><label className="block text-sm font-medium">Preferred tone<select className="mt-2 w-full rounded-xl border border-border bg-white px-3 py-3 text-sm"><option>Professional</option><option>Concise</option><option>Warm and friendly</option></select></label><label className="flex items-center justify-between rounded-2xl border border-border p-4 text-sm"><span><span className="block font-medium">Notion workspace</span><span className="text-xs text-muted-foreground">Prepare pages and tasks for your workspace</span></span><button type="button" className="rounded-full bg-slate-100 px-3 py-1.5 text-xs font-semibold text-slate-700">Connect</button></label><label className="flex items-center justify-between rounded-2xl border border-border p-4 text-sm"><span><span className="block font-medium">Save chat preferences</span><span className="text-xs text-muted-foreground">Keep your tone for this session</span></span><input type="checkbox" defaultChecked className="h-4 w-4 accent-blue-600" /></label></div><button onClick={() => setSettingsOpen(false)} className="mt-6 w-full rounded-xl bg-slate-950 py-3 text-sm font-semibold text-white">Save settings</button></div></div>}</main>
}
